{
  "name": "kacistudio-site",
  "private": true,
  "scripts": {
    "build": "node build-site.mjs",
    "test": "node build-site.mjs && node --test tests/site-regressions.test.mjs"
  },
  "devDependencies": {
    "puppeteer": "25.9.0"
  }
}
